import { WebStackProInbox } from "@/components/dashboard/inbox";

export const metadata = {
  title: "Unified Inbox",
};

/**
 * WebStackPro Unified Inbox page.
 */
export default function DashboardPage() {
  return <WebStackProInbox />;
}