import { WebStackProSettings } from "@/components/dashboard/settings";

export const metadata = {
  title: "Settings",
};

/**
 * WebStackPro Settings page.
 */
export default function SettingsPage() {
  return <WebStackProSettings />;
}